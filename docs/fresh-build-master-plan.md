# WWT Fresh Build Plan

Stack: Next.js 15 + TypeScript + Prisma + Server Actions + useActionState + Zod + JWT + Tailwind

39 pages to build across 5 tiers. One page per prompt. Sequential. Tested before proceeding.

See CLAUDE.md for patterns, rules, and pre-commit checks.
